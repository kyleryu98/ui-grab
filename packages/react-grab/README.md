# Internal Runtime

This package contains the internal runtime source for the `ui-grab` public package.

- Public install instructions live in the repository root [README](https://github.com/kyleryu98/ui-grab#readme).
- End users should install `ui-grab`, not this private workspace package.
- The fork keeps the original React Grab prompt UI and adds built-in `Shift + click` multi-select support.
